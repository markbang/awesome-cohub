# OKP import (later)

Plan: after playbook/concept articles stabilize, create OKP domain **`cohub`** and import cards with `okp-import`.

Do **not** import yet.

Draft schema ideas (subject to change):

```yaml
# future domain README frontmatter sketch
domain: cohub
title: Cohub best practices
types: [principle, concept, playbook, anti-pattern, cheatsheet]
```

Source of truth until then: markdown in this `docs/cohub-is-all-you-need/` tree.
